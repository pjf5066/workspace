<% content_for :title, "Welcome to Open Events" %>

<% content_for :top_content do %>
    <div class="jumbotron">
      <div class="container">
        <div class="middle">
            <h2>Welcome to Open Events</h2>
            <% if !user_signed_in? %>
              <p>Register to get started.</p>
              <p>
                <%= link_to 'Register', new_user_registration_path, class: 'btn btn-primary btn-md' %>
              </p>
            <% else %>
            <% end %>
        </div>
        <div class="col-md-6">
            
        </div>
      </div>
    </div>
<% end %>
